# 📘 คู่มือการตั้งค่าระบบ Gume Kokoro (ล่าสุด)

### 1. การตั้งค่า Google Sheets
- สร้าง Google Sheet และตั้งชื่อ Tab ด้านล่างว่า **"ชีต1"**
- ใส่หัวตารางที่แถว 1: `วัน/เวลา`, `ชื่อ-นามสกุล`, `ระดับ/ชั้นปี`, `แผนกวิชา`, `สรุปผล`

### 2. การติดตั้ง Apps Script
- ไปที่ **Extensions > Apps Script**
- วางโค้ดจาก `Code.gs` ลงไป
- กด **Deploy > New Deployment**
- เลือกประเภท **Web App**
- **Execute as:** `Me`
- **Who has access:** `Anyone` (สำคัญมาก! เพื่อให้เว็บจาก Netlify ส่งข้อมูลเข้าได้)
- กด Deploy และคัดลอก **Web App URL** ไว้

### 3. การเชื่อมต่อกับ Netlify
- นำ URL ที่ได้ไปใส่ใน **Environment Variable** ของ Netlify ชื่อ `DATABASE_URL`
- หรือนำไปวางแทนที่ `DEFAULT_URL` ในไฟล์ `App.tsx`

### 4. การทดสอบ
- ลองทำแบบประเมินในเว็บ
- ถ้าข้อมูลเข้า Google Sheets แสดงว่า POST ทำงานปกติ
- เข้าหน้า Admin (รหัส `167557`) ถ้าเห็นตารางข้อมูลแสดงว่า GET ทำงานปกติ