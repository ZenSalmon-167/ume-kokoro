
# 📘 คู่มือการตั้งค่าระบบ (ฉบับสมบูรณ์)

เพื่อให้ระบบทำงานร่วมกับ Google Sheets ได้อย่างถูกต้อง โปรดทำตามขั้นตอนดังนี้:

### ขั้นตอนที่ 1: เตรียม Google Sheet
1. สร้าง Google Sheet ใหม่
2. ตั้งหัวตารางในแถวที่ 1 (Row 1) ดังนี้:
   - **A1**: `วัน/เวลา`
   - **B1**: `ชื่อ-นามสกุล`
   - **C1**: `ระดับ/ชั้นปี`
   - **D1**: `แผนกวิชา`
   - **E1**: `อายุ`
   - **F1**: `คะแนน`
   - **G1**: `สรุปผล`

### ขั้นตอนที่ 2: ติดตั้ง Code.gs
1. ในหน้า Google Sheet ไปที่ **Extensions** > **Apps Script**
2. ลบโค้ดเดิมออกแล้ววางโค้ดจากไฟล์ `Code.gs` ที่ได้รับ
3. นำ **ID ของ Spreadsheet** (จาก URL) มาใส่ในตัวแปร `SPREADSHEET_ID` ในบรรทัดที่ 8 ของ Code.gs

### ขั้นตอนที่ 3: Deploy (สำคัญมาก)
1. กดปุ่ม **Deploy** > **New Deployment**
2. เลือกประเภทเป็น **Web App**
3. ตั้งค่า:
   - **Execute as**: `Me`
   - **Who has access**: `Anyone`
4. กด **Deploy** และกดยอมรับสิทธิ์ (Authorize access) ให้เรียบร้อย
5. คัดลอกลิงก์ **Web App URL** ที่ได้ มาตรวจสอบว่าตรงกับในโค้ด `pages/Admin.tsx` และ `pages/EmotionCheck.tsx` หรือไม่

### การเข้าใช้งานหน้า Admin
- เข้าไปที่เมนู **Admin** ในหน้าเว็บ
- รหัสผ่านคือ: `167557` (คุณสามารถเปลี่ยนได้ในโค้ด `Admin.tsx` และ `Code.gs`)
